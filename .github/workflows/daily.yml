name: daily-run

on:
  schedule:
      - cron: "30 7 * * 1-5"
        workflow_dispatch:

        jobs:
          run:
              runs-on: ubuntu-latest
                  steps:
                        - uses: actions/checkout@v4
                              - uses: actions/setup-python@v5
                                      with:
                                                python-version: "3.11"
                                                      - name: hello test
                                                              run: echo "hello success"